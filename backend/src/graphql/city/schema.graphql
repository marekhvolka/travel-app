type City {
    id: String
    name: String
    guides: [Guide]
    items: [Item]
}

input CityInput {
    id: String
    name: String
}

type Query {
    fetchCity (id: String): City
    cities: [City]
}

type Mutation {
    updateCity (
        data: CityInput!
    ): City
}
